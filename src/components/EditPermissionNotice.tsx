interface EditPermissionNoticeProps {
  className?: string;
  mode?: "ownerOrAdmin" | "adminOnly";
}

export const EditPermissionNotice = ({
  className: _className,
  mode: _mode = "ownerOrAdmin",
}: EditPermissionNoticeProps) => {
  return null;
};
